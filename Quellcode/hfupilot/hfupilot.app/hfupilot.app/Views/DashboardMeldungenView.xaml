<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:hfupilot.app.ViewModels"
             xmlns:partial="clr-namespace:hfupilot.app.Views.partialview"
             x:Class="hfupilot.app.Views.DashboardMeldungen"
             BackgroundImageSource="{background:EmbeddedImage ResourceId=hfupilot.app.Resource.footer.png}"
             xmlns:background="clr-namespace:ProjectName.MarkupExtensions"
             Title="Aktuelle Meldungen"
             x:Name="Page">
    <ContentPage.Content>
        <StackLayout Margin="0,0,20,20">
            <partial:Navigation BindingContext="{Binding Navigation}" />
            <ListView Margin="20,50" ItemsSource="{Binding Meldungen}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="Datum: " TextColor="Blue"/>
                                <Label Text="{Binding Datum}" TextColor="Blue"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>