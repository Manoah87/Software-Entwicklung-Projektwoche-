<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="hfupilot.app.Views.DashboardTermine">
    <ContentPage.Content>
        <Grid>


            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Entry Grid.Row="1" VerticalOptions="Center" Placeholder="Präparation" Text="{Binding Title}" />
            <Editor Grid.Row="2" VerticalOptions="FillAndExpand" Placeholder="Your description here" Text="{Binding Description}" />
            <Button Grid.Row="3" HorizontalOptions="FillAndExpand" VerticalOptions="End" Command="{Binding SaveCommand}"
                    Text="Save" />
        </Grid>


    </ContentPage.Content>
</ContentPage>